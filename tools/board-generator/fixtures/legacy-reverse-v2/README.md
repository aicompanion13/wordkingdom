# Legacy reverse-direction generated levels

This fixture preserves the 25 generated levels that were active before the
Golden Core Phase 1A direction contract. Some targets use the retired
right-to-left `R` placement. These files are retained only for regression and
migration comparison; normal V3 play must not load them.
